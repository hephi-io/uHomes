{
  "extends": "@uhomes/typescript-config/node.json",
  "compilerOptions": {
    "baseUrl": ".",
    "rootDir": ".", // change this from "src" to "."
    "outDir": "dist",
    "paths": {
      "@uhomes/shared": ["../../packages/shared/src"],
      "@api/*": ["src/*"],
      "@cert/*": ["cert/*"]
    }
  },
  "include": ["src", "tests", "tests/setup.ts"], // keep both source and test included
  "exclude": ["node_modules", "dist"]
}
